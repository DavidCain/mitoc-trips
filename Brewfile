# This aims to enumerate all dependencies for local development on a Mac.
# (One can use Homebrew with Linux too, of course).
brew "asdf"
brew "direnv"

# TODO: Upgrade to newer Postgres once we upgrade RDS
brew "postgresql@14", start_service: true

# Only necessary for Docker-based development
# ===========================================
/* brew "docker", link: true */
/* brew "docker-compose" */
/* brew "colima" */

# We assume that these are provided by `xcode-select --install`
# =============================================================
/* brew "git" */
/* brew "make" */
